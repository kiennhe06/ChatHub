package fpl.ph60001.chathub.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// BẢNG MÀU CHỦ ĐẠO XANH DƯƠNG (BLUE THEME)
// ==========================================

// Màu sắc cho chế độ tối (Dark Mode) - Midnight Premium Blue
val BluePrimaryDark = Color(0xFF0A84FF)       // Xanh dương Neon nổi bật
val BlueSecondaryDark = Color(0xFF64D2FF)     // Xanh dương sáng (Cyan nhẹ)
val BlueTertiaryDark = Color(0xFF30D158)      // Xanh lá (dành cho trạng thái Online)
val BackgroundDark = Color(0xFF0F172A)        // Nền tối Slate 900 siêu sang trọng
val SurfaceDark = Color(0xFF1E293B)           // Bề mặt thẻ Slate 800
val OnPrimaryDark = Color(0xFFFFFFFF)         // Chữ trên màu chủ đạo
val OnBackgroundDark = Color(0xFFF1F5F9)      // Chữ chính trên nền tối
val OnSurfaceDark = Color(0xFFE2E8F0)         // Chữ phụ trên bề mặt tối
val MessageBubbleMeDark = Color(0xFF007AFF)   // Bong bóng chat của tôi (Xanh đậm)
val MessageBubbleOtherDark = Color(0xFF334155) // Bong bóng chat đối phương (Slate 700)

// Màu sắc cho chế độ sáng (Light Mode) - Soft Clean Blue
val BluePrimaryLight = Color(0xFF0066CC)      // Xanh dương đậm đầm chắc
val BlueSecondaryLight = Color(0xFF007AFF)    // Xanh dương chuẩn iOS
val BlueTertiaryLight = Color(0xFF34C759)     // Xanh lá trạng thái Online
val BackgroundLight = Color(0xFFF8FAFC)       // Nền sáng Slate 50 sạch sẽ
val SurfaceLight = Color(0xFFFFFFFF)          // Bề mặt thẻ màu trắng tinh khôi
val OnPrimaryLight = Color(0xFFFFFFFF)        // Chữ trên màu chủ đạo
val OnBackgroundLight = Color(0xFF0F172A)     // Chữ chính trên nền sáng
val OnSurfaceLight = Color(0xFF334155)        // Chữ phụ trên bề mặt sáng
val MessageBubbleMeLight = Color(0xFF007AFF)  // Bong bóng chat của tôi
val MessageBubbleOtherLight = Color(0xFFE2E8F0)// Bong bóng chat đối phương (Slate 200)

// Các màu phụ trợ bổ sung
val GrayLight = Color(0xFF94A3B8)            // Xám phụ trợ
val ErrorColor = Color(0xFFFF453A)            // Đỏ cảnh báo lỗi
val GradientStart = Color(0xFF00C6FF)         // Bắt đầu dải chuyển màu
val GradientEnd = Color(0xFF0072FF)           // Kết thúc dải chuyển màu